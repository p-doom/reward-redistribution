defaults:
  - logger: base_logger
  - arch: anakin
  - system: ppo/ff_ppo
  - network: mlp
  - env: gymnax/cartpole
  - override hydra/launcher: submitit_slurm
  - _self_

hydra:
  searchpath:
    - file://stoix/configs
